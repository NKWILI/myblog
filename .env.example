# Internal Integration Secret from Notion (my-integrations); prefix is usually ntn_
NOTION_TOKEN=
NOTION_DATABASE_ID=
NEXT_PUBLIC_SITE_URL=https://example.com

# Optional: cron-triggered redeploy (refreshes Notion cache on a schedule)
# CRON_SECRET=at_least_16_characters_secret  # Vercel sends this as Bearer token when invoking cron
# VERCEL_TOKEN=your_vercel_api_token          # From Vercel → Settings → Tokens (needed for redeploy API)
# VERCEL_PROJECT_ID=prj_xxx                   # Auto-set on Vercel; add manually if using cron elsewhere
# VERCEL_TEAM_ID=team_xxx                     # Only if using a Vercel team